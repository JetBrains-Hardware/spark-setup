# Examples

## Environment

Copy `.env.example` into your shell environment or load it with your preferred tool.

## SSH Config

See [`ssh_config.example`](ssh_config.example) for a minimal host alias.
